# GENERATED by pazras-timers-gen.sh — do not hand-edit.
# Edit PAZRAS_TIMERS in scripts/core/pazras-master-config.sh, then run:
#   pazras-system timers            (stage into /home/rg/06_SYSTEM/scripts/generated/systemd)
#   pazras-system timers --check    (diff staged vs. live, no writes)
# Applying a staged change to this live file is a human sign-off, not a
# mechanical step — see pazras-timers-gen.sh header.

[Unit]
Description=System health check — stow links, derived-file drift, timer results, mounts

[Timer]
OnCalendar=Wed *-*-* 07:00:00
Persistent=true
RandomizedDelaySec=20m

[Install]
WantedBy=timers.target
